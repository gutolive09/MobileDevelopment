import {Text} from 'react-native'

export default(props) =>{

  // let x = parseInt(props.x)
  // let y = parseInt(props.y)

  return(
    <Text>resultado da soma = {props.x + props.y}</Text>
    )
}